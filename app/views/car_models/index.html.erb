<h1>Modelos de carro</h1>

<!---
<% @car_models.each do |car_model| %>
  <%= render partial: 'car_model', locals: { car_model: car_model } %>
<% end %>
isso é a mesma coisa da linha de baixo, só pode usar isso se tudo tiver o mesmo nome
--->
<%= render @car_models %>

<% if @car_models.empty? %>
  <h2>Nenhum modelo de carro registrado</h2>
<% end %>

<%= link_to 'Registrar um modelo de carro', new_car_model_path %>
<%= link_to 'Voltar', root_path %>